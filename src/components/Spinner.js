import BBspinner from '../img/BBspinner.gif'

const Spinner = () => {
    return(
        <img src={BBspinner} alt='Loading' style={{ width: '200px', margin: 'auto', display: 'block' }} />
    )
}

export default Spinner